{
  "name": "@repo/api-client",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "scripts": {
    "prebuild": "pnpm generate",
    "build": "tsup",
    "dev": "pnpm generate && tsup --watch",
    "lint": "eslint src/",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "generate": "orval && pnpm generate-index",
    "generate-index": "npx barrelsby --config barrelsby.json"
  },
  "dependencies": {
    "@faker-js/faker": "^8.4.1",
    "@tanstack/react-query": "^5.0.0",
    "axios": "^1.6.0",
    "msw": "^2.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^20.0.0",
    "eslint": "^8.0.0",
    "orval": "^6.31.0",
    "tsup": "^8.0.0",
    "typescript": "~5.5.0"
  }
}
