declare module '@repo/jest-config/base' {
  const config: unknown;
  export default config;
}

declare module '@repo/jest-config/react' {
  const config: unknown;
  export default config;
}

declare module '@repo/jest-config/next' {
  const config: unknown;
  export default config;
}